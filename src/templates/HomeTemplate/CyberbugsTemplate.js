import React from 'react';
import Header from '../../components/Home/Header/Header';

export const CyberbygsTemplate = (props) => {
  const { ele: Component, ...restParam } = props;

  return (
    <>

    </>
  );
};
